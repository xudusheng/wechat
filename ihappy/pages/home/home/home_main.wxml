<view>

  <!-- for循环，条件渲染 -->
  <!-- <block wx:for="{{goodsList}}" wx:for-item="goods" wx:key="index">
  
    <view wx:if="{{goods.status===1}}">
      {{goods.goods_title}}
    </view>
    <view wx:elif="{{goods.status===2}}">
      {{goods.goods_title}}
    </view>
    <view wx:else>
      {{goods.goods_id}}
    </view>
  </block> -->


  <!-- <view class="top-tab">
  <view class="top-tab-item {{currentTopItem==idx ? 'active' : ''}}" wx:for="{{topTabItems}}" wx:for-index="idx" data-idx="{{idx}}" bindtap="switchTab">
    {{item}}
  </view>
</view> -->

  <!-- <import src="../../template/empty_page_template.wxml" /> -->
  <!-- <view wx:if="{{data==undefine}}">
    <template is="empty_page" />
    
  </view> -->

  <view class="top-tab">
    <view class="top-tab-item {{homeData.category_list==idx ? 'active' : ''}}" wx:for="{{topTabItems}}" wx:for-index="idx" data-idx="{{idx}}" bindtap="switchTab">

      {{item.name}}
    </view>
  </view>


</view>